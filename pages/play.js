import Head from 'next/head';

export default function PlayPage() {
    return (
        <>
            <Head>
                <title>Play - 960 Dojo</title>
            </Head>
            <main className="page-shell--center">
                <h1 className="page-title page-title--stacked">
                    <span>Play feature will be developed</span>
                    <span>when enough players are using the site regularly.</span>
                </h1>
            </main>
        </>
    );
}
